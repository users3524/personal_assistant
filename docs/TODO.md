## ✅ 已完成

> 以下功能已在历次迭代中完成。

- [x] 简历编辑 — 拖拽排序（已实现，含 ☰ 手柄）
- [x] 分类管理 — 子分类与专属字段拖拽排序（已实现）
- [x] 分类管理 — 大类拖拽排序（已实现，含 reorder 方法）
- [x] 图片保存到系统相册（使用 gal 写入系统 MediaStore）
- [x] 排行榜新增趣味维度（串串榜、缘分榜、把玩王、冷宫幽怨、夜猫子、劳模、端水大师）
- [x] 今日运势 → 文玩老道今日批注（宜/忌双列，日换文案库 20 条）
- [x] 每日随机榜单（每次开盲盒，日限 3 个榜）
- [x] 拟真立体领奖台（阶梯高度差，渐变色台座）
- [x] 月历照片闪高亮动画（跳转后 amber 底色闪烁 3 次）
- [x] 图片路径持久化（图片改为相对路径存储，新增 resolveImageFile 兼容）
- [x] 核桃表单字段兜底（分类模型未加载时使用硬编码默认字段）
- [x] 逾期逻辑重写（纯日期比对 + displayDate 动态漂移 + shouldShowInToday）
- [x] 待办软删除（回收站机制）
- [x] 待办分段查询（活跃/逾期/归档/回收站）
- [x] Dismissible 闪退修复（乐观更新 deleteTodoLocal）
- [x] 简历三模板（简洁经典/现代卡片/技术极简）+ RepaintBoundary 图片导出
- [x] 简历实体扩展（responsibilities / keyDeliverables / badges）
- [x] 待办统计仪表盘（今日完成/本周达成/历史拖延）
- [x] TodoListView 组件提取（双端滑动 + HapticFeedback 震动）
- [x] 代码清理（移除 11 个未使用依赖、删除 9 个空目录、清理死代码）
- [x] 侧滑返回逻辑统一（PopScope 拦截，优先路由 pop）
- [x] 对比图背景包裹渐变 Container，保存时背景一致

---

# 待实现功能清单（TODO）

> 以下功能因复杂度或依赖关系暂未实现。  
> 优先级 P0 = 高（影响核心体验），P1 = 中，P2 = 低（增强功能）。

---

## 排行榜新增更多趣味维度（P2）

**已实现**（串串榜、缘分榜、冷宫幽怨、夜猫子、劳模、端水大师）。如需继续增加：

---

## 月历照片闪高亮（P1）

✅ **已完成**（2026-06，跳转后 amber 底色闪烁 3 次，每次 400ms）

---

## 图片路径持久化（P2）

✅ **已完成**（2026-06，图片改为相对路径存储，新建 `core/utils/image_utils.dart` 兼容新旧）

---

## 旧版格式编辑提示（P2）

**需求**：如果导入了旧版 JSON 数据（`categoryMetadata` 结构不同），编辑界面应检测并提示用户数据格式已变更，展示旧版数据但不可编辑，只有保存更新后才能编辑。

**当前状态**：`categoryMetadata` 为 `Map<String, String>`，核桃类键值形如 `"边宽(mm)": "35.5,36"`（逗号分隔左右值）。旧版可能键名或格式不同。

**涉及文件**：
- `lib/features/collection/presentation/pages/antique_form_page.dart` — `_loadExisting`

---

## 核桃编辑表单保存失败排查（P1 — 需复现后修复）

**需求**：用户反馈核桃表单填写后保存有一定概率失败。怀疑原因：
- `_metaCtrls` 中 "左XX"/"右XX" 键的处理存在边界情况
- `_category == '核桃'` 时 `_currentFields` 为空
- 自定义分类输入框 `onChanged` 与已有 `ChoiceChip` 冲突

**当前状态**：代码已梳理，已加入 `_getDisplayFields()` 兜底逻辑降低风险，但未在真实设备上复现。

---

## 代码结构整理（P3 — 持续优化）

- [x] 移除未使用的 `import` 声明
- [x] 合并部分冗余的 Provider/future 调用
- [ ] 统一错误处理和 `mounted` 检查风格
- [ ] 将常量抽取到独立文件
- [ ] 考虑为共享 Widget 建立公共库

---

## 导出功能扩展（P3）

**当前**：导出为 JSON（含 Base64 内嵌图片）。
**未来考虑**：
- CSV 导出待办/藏品统计
- 导出同时附带 `export.md` 说明文档
- 选择性导出（仅导出/导入某个模块）
- 云备份（iCloud / Google Drive）

---

## 历史版本
- 2026-06-13：v1.0.2 完成，标记 17 项为已完成
- 2026-06-09：创建，记录 v1.0.1 迭代后剩余待实现项
